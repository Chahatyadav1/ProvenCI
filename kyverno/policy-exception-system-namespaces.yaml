apiVersion: kyverno.io/v2beta1
kind: PolicyException
metadata:
  name: exempt-system-namespaces
  namespace: kyverno
spec:
  exceptions:
    - policyName: require-signed-images
      ruleNames:
        - check-image
    - policyName: require-sbom-attestation
      ruleNames:
        - verify-sbom-attestation
  match:
    any:
      - resources:
          kinds:
            - Pod
          namespaces:
            - kube-system
            - kyverno
            - monitoring
            - argocd
